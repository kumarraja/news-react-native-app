/* eslint-disable prettier/prettier */
import React from 'react';
import { Avatar } from 'react-native-paper';

const AvatarIcon = (props) => <Avatar.Icon {...props} icon={props.iconName} />;

export default AvatarIcon;

